#include<stdio.h>

int main(){

    int n;
    scanf("%d" , &n);
    int a[n];
    for(int i=1 ; i<=n ; i++){
        int k;
        scanf("%d" , &k);
        a[k] = i;
    }

    for(int i=1 ; i<=n ; i++){
        printf("%d\n" , a[a[i]]);
    }

    return 0;
}
